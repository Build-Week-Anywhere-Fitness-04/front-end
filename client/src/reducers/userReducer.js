/// react II only
const initalValue = {
  loading: false,
  data: [],
  error: "",
};

export const userReducer = (state = initalValue, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
